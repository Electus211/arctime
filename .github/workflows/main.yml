name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # 允许手动触发

jobs:
  build:  # 修正为 "build"（非 "builds"）
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      # 添加你的实际任务步骤（示例：打印 Hello World）
      - name: Run a script
        run: echo "Hello, GitHub Actions!"
